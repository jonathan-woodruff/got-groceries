/* The page where the user can create a new meal */

import CreateEditMeal from './create-edit-meal';

const CreateMeal = () => {
  return <CreateEditMeal isEditing={false} />
};

export default CreateMeal;